---
title: "a repo name CHOSEN on a SHARED extension must be namespaced"
code: "D3"
family: "Dev-dep & toolchains"
sourceUrl: "https://github.com/tomato-bazel/gate/blob/main/gates/d3_unnamespaced_extension_repo.rq"
---
From the rules_k8s post-mortem, verbatim: "A module extension's repos are
shared across the whole module graph, so `oci.pull(name = "distroless_static")`
here collides with any consumer that pulls a base of that name". Bazel's
error is "A repo named distroless_static_linux_amd64 is already generated by
this module extension". The shipped fix was to prefix with the module name.

Two distinctions carry the whole gate, and getting either wrong makes it
useless:

  * FOREIGN extension only. Apparent repo names are module-scoped, so two
    unrelated modules both using `@crates` is not automatically a conflict —
    only a shared extension instance can collide.
  * CHOSEN names only. `rust_toolchains` is emitted by the rules_rust
    extension for everyone, so nobody is choosing it and there is nothing to
    collide. Flagging it would condemn the documented idiom used by 20+
    modules.

Namespacing is checked against gate:namespacePrefix facts, so `rules_jena`
naming its Maven repo `jena_maven` passes.
