{
  "name": "@tomato-bazel/docs",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "reference": "python3 tools/gen_modules.py --registry .registry --out src/data/modules.json",
    "conformance": "node tools/gen_conformance.mjs --fetch",
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:nosearch": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.0",
    "@astrojs/sitemap": "^3.3.0",
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0",
    "protobufjs": "^8.7.1"
  }
}
